        {
            "@id": "{{ model.name }}",
            "@type": "FloorPlan",
            "spaces": [
                {% for space in model.spaces %}
                "{{ space.name }}"{%- if not loop.last %},
            {% endif %}
                {% endfor +%}
            ]
            {%- if model.wall_openings %},
            "openings": [
                {% for opening in model.wall_openings %}
                "{{ opening.name }}"{%- if not loop.last %},
                {% endif %}
                {% endfor +%}
            ]
            {% endif %}
        }
